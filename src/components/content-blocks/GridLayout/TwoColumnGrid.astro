---
interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<section class:list={["mx-auto max-w-7xl px-6 lg:px-8 grid md:grid-cols-2 justify-between gap-6 md:gap-20", className]}>
  <slot />
</section>
